Paste this to your agent:

```text
The current fisheye dewarp looks like a rectilinear PTZ/center crop. Add a true curvilinear fisheye dewarp mode.

Use the original full-resolution fisheye video texture, not a scaled preview. Configure/detect fisheye center, radiusX/radiusY, FOV, rotation, and lens model. Implement inverse mapping in WebGL: output pixel → 3D ray → source fisheye UV.

Add at least:
- Raw fisheye
- Rectilinear PTZ
- Cylindrical panorama
- Panini/Pannini wide-angle

For wide dewarp, default to cylindrical or Panini, not rectilinear. It is okay for lines/grid squares to curve; the goal is to show more of the full fisheye without looking like a zoomed-in crop.

Also fix blur by matching canvas backing resolution to CSS size × devicePixelRatio, sampling directly from the video texture, and avoiding repeated canvas/CSS rescaling.

Add a debug overlay showing the detected fisheye circle on the raw image.
```
